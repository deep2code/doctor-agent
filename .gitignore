# Binaries
doctor-agent
doctor-agent-linux
*.exe
*.exe~
*.dll
*.so
*.dylib
bin/
dist/

# Test binary
*.test

# Go workspace
go.work

# Environment
.env
*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db


*.csv

# 但允许分割后的语料文件
!external/medical_dialogue/data_split/*.csv

# Build
vendor/
.gocache/
# 项目构建缓存（Go module cache + Python deps，2026-08-30 起收于项目内）
.cache/

# Regenerable raw downloads (scripts in external/*.py rebuild these)
external/medlineplus/
external/msd_manual/
external/msd_manual_prof/
external/position_papers/
external/evalsets/
external/hpo/
external/europepmc/
external/who_factsheets/
external/who_factsheets_zh/

# 中国医学数据外部原始数据（可由 external/fetch_china_*.py 重新抓取）
external/china_cdc/
external/china_clinical_pathways/
external/china_cso/
external/china_dietary/
external/china_stats/
external/china_tcm/

# Z-Anatomy 3D 模型（可再生下载，86MB zip；授权见 external/z-anatomy/ATTRIBUTION.md）
external/z-anatomy/*.zip
external/z-anatomy/*.zip.*
external/z-anatomy/work/
data/media/
external/clinvar/
external/pp_structured/
external/who_factsheets_structured/

# Eval artifacts
evals/report.json
__pycache__/
external/nhc/scanned/

# Regenerable raw downloads (2026-08-09 additions; scripts rebuild these)
external/aap/
external/fhs/pages/
external/immunization/

# Pipeline intermediates & logs (regenerable; scripts in external/*.py)
external/cdc/jkts/
external/cdc_structured/
external/cdc_struct.log
external/dailymed/
external/dailymed_fetch.log
external/dailymed_struct.log
external/dailymed_structured/
external/eml/

.cowork-temp/
.workbuddy/
remote-deploy.sh
.dumate/

# 脚本目录（本地工具脚本，不需要版本管理）
scripts/

# bake-local.sh 产物（烘焙后 COPY 进 Docker 镜像，不需要版本管理）
qdrant-storage/

# 本机 ONNX 导出产物（bge-m3 fp32 约 2.3GB，external/export_onnx.py 生成）
bge-m3-onnx/
external/statpearls/
external/lactmed/
external/gomodcache/
external/medgen/
external/icd11/
external/.cache/
internal/knowledge/data/corpus_statpearls.json
logs/*.log

# 下载的原始 PDF 素材 (可由 external/fetch_medlinezh.py 重新下载)
external/medlinezh/
external/otc_labels/raw/
# KB 数据镜像的 dump 产物 (397MB, 由 build.sh kb 从本地库导出)
docker/kb/init-doctor_knowledge.sql.gz

# 超过 GitHub 单文件 100MiB 上限的源文件：整文件只留本地，仓库里提交的是分片
# (external/split_data.py split/merge/verify；make_gz.py 会自动按分片合并构建 gz/)
internal/knowledge/data/huatuo_qa.json
internal/knowledge/data/medical_qa_pairs.json
# CMB 原始压缩包与解压产物（评测集由 external/convert_cmb.py 从中挑选后生成）
external/cmb/
# Orphanet 原始 XML 产品包（约 280MB，由 external/convert_orphanet.py 消费，可从 Orphanet 重下）
external/orphanet/

# 第三批科普扩充（2026-09-23）的原始 PDF/全文素材：体积大且可由各 raw/MANIFEST.txt 记录的官方直链重下；
# 保留 .txt 文字层摘录与 MANIFEST.txt 作为入库数字的溯源依据
external/checkup_labs/raw/*.pdf
external/chronic_diet/raw/*.pdf
external/chronic_diet/full/
external/safe_medication/raw/*.pdf
external/infection_food/raw/*.pdf
external/infection_food/raw/*.docx

# 第四批科普扩充（2026-09-24）居家自测与慢病随访：原始 PDF 与未清洗的重复文字层不入库，
# 保留 *_clean.txt（官方 PDF 文字层）与 curated 摘录 + MANIFEST.txt 作溯源
external/home_monitoring/raw/*.pdf
external/home_monitoring/raw/t2dm_prevention_2024_full.txt
external/home_monitoring/raw/jiating_xueya_2019.txt
external/home_monitoring/raw/jiceng_hypertension_2025.txt

# 第五批科普扩充（2026-09-24）的原始 PDF 素材：可由各 raw/MANIFEST.txt 记录的官方直链重下；
# 保留 raw/*.txt 逐字摘录与 MANIFEST.txt 作为入库数字的溯源依据
external/eye_ent_health/raw/*.pdf

# 第六批科普扩充（2026-09-25）方向 B/C/D 的原始 PDF 素材，口径同上（只入库 txt 摘录 + MANIFEST）
external/child_injury/raw/*.pdf
external/exam_safety/raw/*.pdf
external/home_environment/raw/*.pdf

# 批量下载正文库（2026-09-24~25 建的四个源）：整目录是可再生缓存，重跑同名 fetch 脚本即可重建，
# 一律不入库（gov_health 实测 1.7GB / 378 份 PDF + 1872 篇正文；piyao 14.5MB；jkb 5.0MB）。
# 脚本自身（external/fetch_gov_health.py、fetch_gov_annex.py、fetch_piyao.py、fetch_jkb.py）提交。
external/gov_health/
external/piyao_health/
external/jkb_health/
